# 🏥 基於隨機森林（Random Forest）演算法的心臟病臨床特徵預測與特徵重要性評估系統

本專案為機器學習實務期中專案，旨在利用集成學習（Ensemble Learning）算法對真實臨床醫療數據進行分類預測，並透過可解釋性 AI（XAI）技術評估各項生理指標對心臟病的影響權重。

---

## 📌 專案摘要 (Abstract)

本專案採用學術界公開的 **UCI 心臟病資料集（Heart Disease Dataset）** 統計特徵，內含真實患者的臨床生理指標（如年齡、血壓、膽固醇、最大心率等）。我們實作了**隨機森林（Random Forest）** 分類器，透過自助法（Bootstrapping）與特徵隨機選擇機制克服單一決策樹容易過擬合（Overfitting）的缺點。實驗結果顯示，本模型在測試集上達到了優異的分類準確度，並透過基尼不純度（Gini Impurity）成功定量分析出關鍵預測特徵。

---

## 🛠️ 系統架構與資料預處理說明

本專案的機器學習管線（Pipeline）完全依照標準資料科學流程建立：

1. **真實資料集特徵 (Features)**
   * `age`: 患者年齡
   * `sex`: 性別（0 = 女性, 1 = 男性）
   * `cp_pain_type`: 胸痛類型（分 4 類）
   * `trestbps`: 靜態血壓
   * `chol`: 血清膽固醇
   * `thalach`: 達到的最大心率
   * `oldpeak`: ST段下降值（心電圖指標）

2. **資料預處理流程**
   * **資料集隨機劃分**：使用 `train_test_split` 演算法，以 **80% 訓練集** 與 **20% 測試集** 的比例進行隨機抽樣分流。此舉旨在保留完全未參與訓練的測試集進行客觀盲測，防止模型產生過擬合。
